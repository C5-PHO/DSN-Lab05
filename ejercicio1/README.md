# Ejercicio 1

```bash
docker compose up -d
docker compose ps
docker compose logs web
```

Abre `http://localhost:8080`. Modifica `html/index.html` y recarga el navegador para comprobar el bind mount.

```bash
docker compose down
```
